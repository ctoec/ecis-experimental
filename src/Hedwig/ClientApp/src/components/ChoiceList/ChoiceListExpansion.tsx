import React from 'react';

type ChoiceListExpansionProps = {
	showOnValue: string;
};

export const ChoiceListExpansion: React.FC<ChoiceListExpansionProps> = ({ children }) => {
	return <>{children}</>;
};
